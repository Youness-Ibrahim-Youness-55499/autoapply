DATABASE_URL=sqlite:///./jobman_jobs.db
# Production example (keep this only in the worker host's secret store):
# DATABASE_URL=postgresql://postgres.PROJECT_REF:PASSWORD@HOST:5432/postgres?sslmode=require
TARGET_COUNTRY=DE
CRAWLER_USER_AGENT=JobDatabaseLab/0.1 (+local-test; contact=you@example.com)
PER_DOMAIN_DELAY_SECONDS=1.0
INACTIVE_AFTER_DAYS=7
