import type { TemplateKind } from "./useTemplates";

export const templateKindLabelKeys: Record<TemplateKind, string> = {
  cover_letter: "editor.kind.cover_letter",
  paragraph: "editor.kind.paragraph",
  screening: "editor.kind.screening",
};
